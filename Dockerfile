# Базовая система с Node.js и NPM
FROM node:lts

# Рабочая директория внутри контейнера
WORKDIR /app

# Копируем весь проект внутрь рабочей директории
COPY . .

# Устанавливаем зависимости
RUN npm install

# Сборка приложения
RUN npm run build

# Команда запуска сервера
CMD ["npm", "run", "dev"]